# google-genai-sdk-chat-memory
This example demonstrates how to build a simple conversational AI agent using the Google Generative AI SDK (Gemini API) that maintains context across multiple turns. It showcases the 'long-lived' aspect by leveraging the chat session's built-in memory to provide coherent and context-aware responses, mimicking an agent that learns and adapts within 
